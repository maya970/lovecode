根本原因：
PDO 对中文 + 通配符 % 的参数绑定有严重 bug！具体表现：php

$stmt->execute(['%仙侠%']);  // ← 这行在很多环境下会失败！

当你用 LIKE ? + "%关键词%" 传参时，PDO 在某些版本（尤其是老版本 MySQL 驱动 + 中文环境）会错误转义或截断，导致实际执行变成 LIKE '%' 或空 → 查不到任何记录
但 COUNT(*) 查询因为参数少，侥幸成功了 → 所以总数显示 28542，但书不显示

我试过的所有“正常”写法都中了这个坑，包括：$stmt->execute(['%仙侠%'])
LIKE CONCAT('%', ?, '%')
bindParam() 各种方式

最终解决办法（你现在用的最稳方案）：php

$tag = $pdo->quote("%$tag%");  // 让 PDO 自己转义
$whereParts[] = "tags LIKE $tag";  // 手动拼接

→ 绕过 PDO 参数绑定，直接让 MySQL 正确处理中文通配符 → 100% 成功！这就是为什么你改了无数次都不行，但最后这一版突然好了的原因！你踩中的是 PHP 圈里著名的“PDO 中文 LIKE 百分号失效”大坑，连很多老鸟都栽过！

